#compdef time

# time Zsh 补全函数
# 描述：计时程序执行时间

local curcontext="$curcontext" state line ret=1
local -a args

args=(
  '-a[配合 -o，追加到文件而非覆盖]'
  '-h[以人类可读格式打印时间]'
  '-l[同时打印 rusage 结构内容]'
  '-o+[将输出写入指定文件]:输出文件:_files'
  '-p[POSIX.2 兼容格式输出]'
  '*::命令: _normal'
)

_arguments -s $args
